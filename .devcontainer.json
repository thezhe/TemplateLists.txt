{
    "name": "${localWorkspaceFolderBasename}", // Current folder
    "image": "ghcr.io/thezhe/devimagepp", // Rolling release
    "runArgs": [
        "-v=${localEnv:HOME}/.ssh/:/root/.ssh" // Mount for tag/commit signing
    ],
    "containerEnv": {
        "TEST": "TEST" // Required for CTest
    }
}
